let primitive = 1;

if (typeof (primitive) === "number") {
    console.log(primitive + " : " + typeof (primitive));
} else if (typeof (primitive) === "string") {
    console.log(primitive + " : " + typeof (primitive));
} else {
    console.log(primitive + " : " + typeof (primitive));
}
