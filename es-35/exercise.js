function canPlay() {
  let sport = " Football";
  let personName = "Cosimo";

  // if (true) {
  //   let personName = "Cosimo";
  // }

  console.log(personName + sport);
}

canPlay();

/*
When defining a variable inside an if statement, it is considered a local
scope which is only defined in the if statement block and can't be called
from the outside.
*/
