const primitive = 5;

switch (typeof (primitive)) {
    case "number":
        console.log(primitive + " : " + typeof (primitive));
        break;
    case "string":
        console.log(primitive + " : " + typeof (primitive));
        break;
    case "boolean":
        console.log(primitive + " : " + typeof (primitive));
        break;
}
